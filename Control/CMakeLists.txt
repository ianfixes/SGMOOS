SET(CONTROL_DIRS SeaGrantTaskLib)

SET(BUILD_SG_PHELM ON CACHE BOOL "Build pHelmSG")
IF(BUILD_SG_PHELM)
    SET(CONTROL_DIRS ${CONTROL_DIRS} pHelmSG)
ENDIF(BUILD_SG_PHELM)

SET(BUILD_SG_PLUAHELM ON CACHE BOOL "Build Lua Helm")
IF(BUILD_SG_PLUAHELM)
    SET(CONTROL_DIRS ${CONTROL_DIRS} pLuaHelm)
ENDIF(BUILD_SG_PLUAHELM)

IF(CONTROL_DIRS)
    SUBDIRS(${CONTROL_DIRS})
ENDIF(CONTROL_DIRS)
