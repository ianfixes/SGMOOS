SET(SGMISCAPPS_DIRS)

SET(BUILD_SG_PSYSTEMHEALTH ON CACHE BOOL "Build pSystemHealth system monitor")
IF(BUILD_SG_PSYSTEMHEALTH)
    SET(SGMISCAPPS_DIRS ${SGMISCAPPS_DIRS} pSystemHealth)
ENDIF(BUILD_SG_PSYSTEMHEALTH)

SET(BUILD_SG_PIMAGEENHANCE OFF CACHE BOOL "Build pImageEnhance image processor")
IF(BUILD_SG_PIMAGEENHANCE)
    SET(SGMISCAPPS_DIRS ${SGMISCAPPS_DIRS} pImageEnhance)
ENDIF(BUILD_SG_PIMAGEENHANCE)

SET(BUILD_SG_PLOGMYSQL OFF CACHE BOOL "Build pLogMySQL data logger")
IF(BUILD_SG_PLOGMYSQL)
    SET(SGMISCAPPS_DIRS ${SGMISCAPPS_DIRS} pLogMySQL)
ENDIF(BUILD_SG_PLOGMYSQL)

SET(BUILD_SG_PPIPENAV OFF CACHE BOOL "Build pPipeNav pipe navigator")
IF(BUILD_SG_PPIPENAV)
    SET(SG_MISCAPPS_DIRS ${SGMISCAPPS_DIRS} pPipeNav)
ENDIF(BUILD_SG_PPIPENAV)


IF(SGMISCAPPS_DIRS)
    SUBDIRS(${SGMISCAPPS_DIRS})
ENDIF(SGMISCAPPS_DIRS)

