SET(SGINSTRUMENT_DIRS)

SET(BUILD_SG_ACTUATION ON CACHE BOOL "Build SG Actuation: iMotor, iActuationSG")
IF(BUILD_SG_ACTUATION)
    SET(SGINSTRUMENT_DIRS ${SGINSTRUMENT_DIRS} Actuation)
ENDIF(BUILD_SG_ACTUATION)

SET(BUILD_SG_MULTISONDE ON CACHE BOOL "Build iMultisonde: covers Seabird, hydrolab, YSI")
IF(BUILD_SG_MULTISONDE)
    SET(SGINSTRUMENT_DIRS ${SGINSTRUMENT_DIRS} iMultiSonde)
ENDIF(BUILD_SG_MULTISONDE)

SET(BUILD_SG_BATTERY ON CACHE BOOL "Build iBatterySG")
IF(BUILD_SG_BATTERY)
    SET(SGINSTRUMENT_DIRS ${SGINSTRUMENT_DIRS} iBatterySG)
ENDIF(BUILD_SG_BATTERY)

SET(BUILD_SG_ICAMERA OFF CACHE BOOL "Build iCamera - requires libDC1394-1.3")
IF(BUILD_SG_ICAMERA)
    SET(SGINSTRUMENT_DIRS ${SGINSTRUMENT_DIRS} iCamera)
ENDIF(BUILD_SG_ICAMERA)

SET(BUILD_SG_IVISION OFF CACHE BOOL "Build iVision processing suite")
IF(BUILD_SG_IVISION)
    SET(SGINSTRUMENT_DIRS ${SGINSTRUMENT_DIRS} iVision)
ENDIF(BUILD_SG_IVISION)

SET(BUILD_SG_IRELAY OFF CACHE BOOL "Build iRelay")
IF(BUILD_SG_IRELAY)
    SET(SGINSTRUMENT_DIRS ${SGINSTRUMENT_DIRS} iRelay)
ENDIF(BUILD_SG_IRELAY)

IF(SGINSTRUMENT_DIRS)
    SUBDIRS(${SGINSTRUMENT_DIRS} MOOS_Branched)
ENDIF(SGINSTRUMENT_DIRS)
