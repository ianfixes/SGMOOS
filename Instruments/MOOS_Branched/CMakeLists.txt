SET(MOOS_SG_BRANCHED_DIRS)

SET(BUILD_SG_DVL ON CACHE BOOL "Build iDVL_SG")
IF(BUILD_SG_DVL)
    SET(MOOS_SG_BRANCHED_DIRS ${MOOS_SG_BRANCHED_DIRS} iDVL_SG)
ENDIF(BUILD_SG_DVL)

SET(BUILD_SG_INS ON CACHE BOOL "Build iINS_SG")
IF(BUILD_SG_INS)
    SET(MOOS_SG_BRANCHED_DIRS ${MOOS_SG_BRANCHED_DIRS} iINS_SG)
ENDIF(BUILD_SG_INS)

SET(BUILD_SG_IRANGE ON CACHE BOOL "Build iRange SG")
IF(BUILD_SG_IRANGE)
    SET(MOOS_SG_BRANCHED_DIRS ${MOOS_SG_BRANCHED_DIRS} iRange)
ENDIF(BUILD_SG_IRANGE)

SET(BUILD_SG_GPS ON CACHE BOOL "Build iGPS_SG, which actually reports lat/lon")
IF(BUILD_SG_GPS)
    SET(MOOS_SG_BRANCHED_DIRS ${MOOS_SG_BRANCHED_DIRS} iGPS_SG)
ENDIF(BUILD_SG_GPS)

if(MOOS_SG_BRANCHED_DIRS)
    SUBDIRS(${MOOS_SG_BRANCHED_DIRS})
ENDIF(MOOS_SG_BRANCHED_DIRS)
